<div class="container">
<ul>
  <% for user in @users %>
  <li id="user_<%= user.id %>"><%=user.name %> 
    | <%= link_to 'Edit', edit_user_path(user) %>
    | <%= link_to 'Delete', user, :method => 'delete' %>
  </li>
  <% end %>
</ul>
</div>
