<div class="container">
<h1>Posts</h1>
<%=render 'post_form' %>
<ul>
  <% for post in @posts %>
  <li id="post_<%= post.id %>"><%= post.title %>
    | <%= link_to 'Edit', edit_post_path(post) %>
    | <%= link_to 'Delete', post, :method => 'delete' %>
  </li>
  <% end %>
</ul>
<div>
